const { Router } = require('express');
// const { GovernateController } = require('../controllers/governateContoller');
//   GovernateController.getAllGovernates(req, res);
//   GovernateController.getGovernateCities(req, res);

const router = Router();

router.get('/', (req, res) => {
});

router.get('/amenities', (req, res) => {
});

router.get('/:amenityID', (req, res) => {
  // get all the rooms relating featuring this amenity?
});
